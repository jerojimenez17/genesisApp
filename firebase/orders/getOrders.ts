// export const getOrders
// getDoc(doc(fbDB, `clients/${data.client}`)).then((data) => {
//     if (data.exists()) {
//       const adaptedData = ClientFirebaseAdapter.fromDocumentData(
//         data.data(),
//         dataId
//       );
//       order.client = adaptedData;
//     }
//   });
